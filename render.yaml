services:
  - type: web
    name: relay-messaging-engine
    runtime: docker
    plan: free
    region: frankfurt
    healthCheckPath: /healthz
    autoDeployTrigger: commit
    envVars:
      - key: TRUST_PROXY
        value: "true"
